spring:
  application:
    name: rachael-api-user
  cloud:
    config:
      uri: http://localhost:8888
      label: development
  config:
    import: "configserver:"

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true
    registry-fetch-interval-seconds: 10  # default è 30
  instance:
    preferIpAddress: false
    hostname: rachael-api-user
